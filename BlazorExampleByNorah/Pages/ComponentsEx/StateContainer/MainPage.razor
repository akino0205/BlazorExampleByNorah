@page "/ComponentEx/StateContainer"
@inject AppStateForComponent AppState
@implements IDisposable

@AppState.SelectedColour

<RedComponent />
<BlueComponent />

@code {

    protected override void OnInitialized()
    {
        AppState.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        AppState.OnChange -= StateHasChanged;
    }

}